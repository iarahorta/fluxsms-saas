{
  "name": "fluxsms-polo-worker-draft",
  "version": "0.1.0",
  "private": true,
  "description": "Rascunho Electron — Polo: Partner API + COM + heartbeat + fila SMS",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "rebuild": "electron-rebuild -f -w serialport"
  },
  "dependencies": {
    "axios": "^1.6.8",
    "electron-store": "^8.2.0",
    "serialport": "^12.0.0"
  },
  "devDependencies": {
    "@electron/rebuild": "^3.6.0",
    "electron": "^28.3.3"
  }
}
