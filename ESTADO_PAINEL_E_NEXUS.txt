ESTADO DO PROJETO (Cursor) — 2026-04-24
========================================

Pausa intencional em NEXUS / webhooks de gateway
-------------------------------------------------
- Prioridade: painel de números, estoque, botão Solicitar, compra com saldo.
- Não retomar Nexus (PIX, criar-pix-nexus, chaves NEXUSPAG) até o painel estar 100% validado em produção.
- Quando o painel estiver ok, fazer backup (tag/commit) e só então perguntar se pode voltar à integração Nexus.

O que foi entregue nesta fase (técnico)
--------------------------------------
- GET /api/public/estoque — plano B com service_role, sinal 60 min (last_ping / ultima_comunicacao), sem exigir polo "ONLINE" no front.
- Front: loadChipsCount funde API pública + rpc_get_service_stocks + fallback tabela chips; PIX falho só avisa, não quebra o resto.
- SQL Supabase: migrations/024_rpc_v3_chip_offline_com_sinal_60m.sql — venda/estoque com chip "offline" mas vivo nos últimos 60 min.
- server.js v2.1.28

Fase actual (2026-04-25)
-----------------------
- **Painel cliente: CONGELADO** — não alterar lógica; referência: tag `backup-painel-cliente-ok-2026-04-25` e `BACKUP_FASE_2026-04-25_painel_cliente_congelado.txt`.
- Trabalho em curso: **só admin** — contador Chips ON alinhado ao .exe (ultima_comunicacao + janela 60 min p/ last_ping em offline).

Próximo passo humano
--------------------
- Após pull/deploy: reabrir admin e comparar "Chips ON" com o .exe.

Atualização (2026-04-25 13:10 BRT)
----------------------------------
- Produção estabilizada no Railway (último deploy SUCCESS): `21d2006b-680d-4966-a49a-4007b302ca6e`.
- Erro 502 de host resolvido (módulos/rotas restaurados e exports de rate limit corrigidos).
- Estoque cliente mudou para modo estrito operacional:
  - só conta chips ONLINE com `last_ping <= 3 min`;
  - endpoint validado em produção com HUB desligado: `chips_vivos=0`.
- RPC de compra continua com segurança de usuário por sessão (`auth.uid()`) e sem uso de coluna inexistente (`c.updated_at` removido).
- Novo endpoint de shutdown operacional adicionado: `POST /sms/shutdown` (força OFFLINE por `polo_key`).

Nexus / PIX (status actual)
---------------------------
- Chave Nexus no Railway responde (teste de autenticação OK; retorno 400 de validação de payload inválido).
- Se QR continuar vazio no front, usar logs do backend para inspecionar resposta exata da Nexus (já instrumentado em `lib/nexusPag.js`).

Pendência humana crítica (Supabase SQL Editor)
----------------------------------------------
- Aplicar no projeto Supabase correto:
  1) `supabase/SQL_PASTE_CANONICAL_rpc_solicitar_v3_4args.sql`
  2) `supabase/migrations/026_activations_fk_integrity_hardening.sql`
  3) `NOTIFY pgrst, 'reload schema';`
